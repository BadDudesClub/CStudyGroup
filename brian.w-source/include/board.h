#ifndef ___BOARD_
#define ___BOARD_

int insert_bit(int index, int store, int bit) {

    if(0 > index || index > 16)
        return -1;


    if(0 > bit || bit > 1)
        return -1;


    /*
    int i;
    for(i = 0; i < length; i++){
        printf("%c \n", input_string[i]);
    }
    */

    return 1;
}

#endif